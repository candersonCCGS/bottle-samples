TO DO:
- tidy up create database and insert data response pages
- change insert_data so it reads directly form the csv files to insert insert_data
- add to GitHub
- add comments
- use rebase?
- create a dictionary (or record) for each query with the title, description and query as elements

For basic app:
- have separate pages for each database create and insert outcome
